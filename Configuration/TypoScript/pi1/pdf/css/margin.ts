plugin.tx_pdfcontroller_pi1 {
    // empty statement for proper comments only
  pdf {
  }
    // css
  pdf =
  pdf {
      // empty statement for proper comments only
    css {
    }
      // margin
    css =
    css {
        // empty statement for proper comments only
      margin {
      }
        // div, h1, h2, h3, ol, p, ul
      margin =
      margin {
          // bottom, top
        div =
        div {
            // value, times
          bottom =
          bottom {
            value = 0
            times = 0
          }
            // value, times
          top =
          top {
            value = 0
            times = 0
          }
        }
          // bottom, top
        h1 < .div
          // bottom, top
        h2 < .div
          // bottom, top
        h3 < .div
          // bottom, top
        ol < .div
          // bottom, top
        p < .div
          // bottom, top
        ul < .div
      }
    }
  }
}