plugin.tx_pdfcontroller_pi2 {

  # cat=PDF Controller Button//110;       type=int+;    label= ID PDF Controller:Id of the page, which contains the PDF Controller User Interface
  page.pdf_controller =

  # cat=PDF Controller Button//120;       type=int+;    label= typeNum for print template:default is 98
  typeNum.print = 98

  # cat=PDF Controller Button//130;       type=string;  label= Print page:realUrl name for pages with the typeNum.print (see above). Name depends on your realURL configuration. Example: print.html
  realUrl.print = 

}